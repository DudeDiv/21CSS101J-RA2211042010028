# include<stdio.h>
   int main() {
    //area of circle
      float radius;
      printf("What's the radius?");
      scanf("%f", &radius);
      printf("Hmm, ok, area is : %f", 3.14*radius*radius);
      return 0;
}
